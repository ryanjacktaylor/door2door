<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference"/>
        <attr name="metaButtonBarButtonStyle" format="reference"/>
    </declare-styleable>

    <declare-styleable name="SegmentedControl">
        <attr name="numSegments" format="integer" />
        <attr name="segmentText1" format="string" />
        <attr name="segmentText2" format="string" />
        <attr name="segmentText3" format="string" />
        <attr name="segmentText4" format="string" />
        <attr name="activeBackgroundColor" format="color" />
        <attr name="inactiveBackgroundColor" format="color" />
        <attr name="activeTextColor" format="color" />
        <attr name="inactiveTextColor" format="color" />
        <attr name="textSize" format="dimension" />
    </declare-styleable>

</resources>
